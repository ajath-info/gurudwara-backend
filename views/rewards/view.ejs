<div class="space-y-6 p-6">
  <div class="flex items-center justify-between">
    <h1 class="text-2xl font-bold text-gray-900">
      Reward Details - <%= selectedReward.title %>
    </h1>
    <a href="/admin/rewards" class="text-blue-600 hover:text-blue-900"
      >Back to Rewards</a
    >
  </div>

  <div class="bg-white rounded-lg shadow-lg max-w-2xl w-full p-6">
    <div class="space-y-4">
      <div class="aspect-w-16 aspect-h-9">
        <img
          src="<%= selectedReward.image_urls && selectedReward.image_urls.length > 0 ? selectedReward.image_urls[0] : '/images/default-reward.jpg' %>"
          alt="<%= selectedReward.title %>"
          class="w-full h-64 object-cover rounded-lg"
          onerror="this.src='/images/default-reward.jpg'"
        />
      </div>
      <div>
        <h3 class="text-xl font-semibold text-gray-900 mb-2">
          <%= selectedReward.title %>
        </h3>
        <p class="text-gray-600 mb-4"><%= selectedReward.description %></p>
      </div>
      <div class="grid grid-cols-2 gap-4">
        <div>
          <label class="block text-sm font-medium text-gray-700"
            >Points Required</label
          >
          <p class="mt-1 text-sm text-gray-900"><%= selectedReward.points %></p>
        </div>
        <div>
          <label class="block text-sm font-medium text-gray-700">Status</label>
          <div class="mt-1">
            <% if (selectedReward.status === '1') { %>
            <span
              class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800"
              >Active</span
            >
            <% } else { %>
            <span
              class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-red-100 text-red-800"
              >Inactive</span
            >
            <% } %>
          </div>
        </div>
        <div>
          <label class="block text-sm font-medium text-gray-700"
            >Gurudwara</label
          >
          <p class="mt-1 text-sm text-gray-900">
            <%= selectedReward.gurudwara_name %>
          </p>
        </div>
        <div>
          <label class="block text-sm font-medium text-gray-700">Created</label>
          <p class="mt-1 text-sm text-gray-900">
            <%= new Date(selectedReward.created_at).toLocaleDateString() %>
          </p>
        </div>
      </div>
    </div>
  </div>
</div>
